# Session Review Snapshots

Historical generated review dossiers that used to live under `tests/eval/generated/`.

## Policy

- Keep snapshots here only when they need to be preserved for comparison.
- Fresh evaluation output should be generated into `tests/eval/generated/` and treated as disposable.
