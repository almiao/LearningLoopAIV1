# PRODUCT — Learning Loop AI

> 本文件是产品定位与主流程的单一事实源（single source of truth）。
> 开发取舍、信息架构、开源 README 都以此为锚。与本文件冲突的旧设计以本文件为准。

---

## 一句话

**把你的文档、JD、笔记，练成你真能讲出来的东西 —— 先从拿下你的下一场技术面试开始。**

- **身份（它是什么）**：一个 mastery 引擎。能长期自用、开源干净、不会"用完即弃"。
- **钩子（怎么拉人 / demo）**：技术面试准备。痛点尖、决策强、结果可验证、截图锐利。
- 二者不是二选一，是两层：**身份决定留存，钩子决定拉新**。同一套引擎、同一套代码，区别只在首句文案——文案随时可改，零成本。所以不要在文案上耗，先把引擎做出来。

---

## 0. 先定两个风险决策（在画流程、写代码之前）

整个产品的价值压在"判定你是否真的掌握"上。这一步绕不开 LLM，又是最容易静默失真的环节。**先把这两件定死，否则后面那条漂亮闭环会自我欺骗。**

### 0.1 判分锚定 — 决定 mastery state 是否可信
- 判分针对**当前这道题**生成一组"过线必须命中的点"（**一次性判分锚点**：判完即弃、不入库、不复用，见术语表），**不预先给每个概念铺锚点**（§1.6 / §4：不预拆文档）。
- LLM 判分输出 **"命中了哪几个锚点 / 漏了哪几个"**，而不是一个裸分数。**漏掉的锚点就是复习项的种子**（§4）。
- 低置信 → 标记 `存疑`，不猜。确定性状态层**只接受可解释的信号**。
- 默认防"判太松"：宁可判 `未过` 也不放水。否则掌握度虚高 → 复习队列不再推该推的点 → 整个闭环静默失效。

### 0.2 输入模态 — 决定"会背不会讲"是否测得出
- 核心机制 = **提交后不可编辑（一次性提交）**：逼出"一口气讲"的真实状态。松弛、可反复编辑的 textarea 会把"不会讲"伪装成"会讲"，直接废掉核心卖点。这条**日常常开**——但它**不靠倒计时**，靠"提交即定稿、想补充就回答下一道追问"，对日常学习者不构成压力。
- **可见倒计时是面试模式的压力参数，日常默认关**：日常主动学习是好奇驱动的留存活动，一个滴答的钟会把它变成考试、反伤留存。时间可**静默记录**当账本软信号，但不在日常 UI 施压。倒计时只在面试 / 模拟面打开（§3③ 压力参数）。
- **语音是可选 Labs，不是核心依赖。**

---

## 1. 核心原则

1. **两层一引擎**：日常底座（mastery loop over 你的资料）+ 面试战役（同一引擎的参数化）。面试不是第二个产品。
2. **LLM 生成，状态确定**：LLM 负责出题 / 追问 / 对抗 / 按一次性锚点判分；**记忆、复习调度、进度、"是否真的掌握"的最终状态由确定性系统持有**，可移植、可审计、可开源。
3. **单引擎护栏（成败判据）**：面试永远只是 loop 的参数化，**绝不允许长出独立引擎或独立状态库**。一旦它开始需要自己的引擎/存储，说明兼顾失败，砍掉一个，别两个都喂。
   - **护栏管的是引擎 / 状态，不是 UI**：面试战役**可以有自己的页面**——它是 campaign 型 Goal 的**仪表盘 / 详情视图**（倒计时 + 覆盖率 + 就绪度 + 模拟入口 + 面后回灌），是对**共享引擎 + 共享账本**的一个 scoped 视图。真 drill 时仍**落进同一个 Practice、失败仍进同一个账本**。
   - **安全测试**：那页面**自带评分逻辑 / 自带状态库 / 自带 drill 回路**的那一刻 = 第二个产品在长，立刻塌回参数化。判据永远是"有没有独立引擎/状态"，不是"有没有独立页面"。
4. **价值随模型迁移**：文档问答 / 总结 / 出题正被基座碾平且持续**贬值**；跨时间的状态 / 记忆是 LLM 结构性补不上的洞，在**升值**。资源往后者堆。
5. **竞品空位**：`source-grounded reading + adversarial drill + inspectable mastery state`。
   - 不和 Obsidian 抢存储，做它**下游的掌握层**（任意 markdown / 文件夹 / vault 当输入源）。
   - 定位坐标 = **"会自动制卡 + 会像面试官一样追问的 Anki"**。
   - **源层 vs 掌握层纪律**：文档是**源**（像 Obsidian 笔记），复习项是**卡**（像 Anki 卡）。源层只存**导航**（读到哪、链到谁），**从不存掌握**；掌握只活在账本，是失败点的派生视图。给文档挂 per-doc"已掌握"标记 = 把这两层缝错（详见 §4）。
6. **建模失败，不建模资料（自下而上）**：不预先把文档拆成概念图。decomposition / 本体 / 去重是知识图谱类产品的死法，而且正好是 §3 里已判定低价值的 Plan 重活。模型的单元是"你实际答崩的点"，在 Practice 暴露的那一刻才生成。文档始终是被检索的原文，不被建模。详见 §4。

---

## 术语 & 实体纪律（先读这个）

> 判据：一个东西要不要当"实体"来存，只问一句——**它需要跨时间被匹配 / 去重 / 维护吗？**
> 需要 → 是结构负担，越少越好；生成→用→弃、只活在一次调用里 → 免费。
> 全产品**只有两个持久实体**，其余一律一次性 / 派生视图 / 指针。违反这条 = 在偷偷重建本体。

| 名词 | 类别 | 含义 / 纪律 |
|---|---|---|
| **Goal** | **持久实体 ①** | 日常目标 或 面试战役。身份由用户声明、数量就几个，不从内容自动抽 → 无去重负担 |
| **复习项 review item** | **持久实体 ②** | 唯一从内容侧长出的持久单元；**失败驱动**——答崩才生。全局，不归属任何 Goal |
| **一次性判分锚点**（旧称 rubric） | 一次性 | 判**某一道题**时生成的"该碰到的点"，**判完即弃、不入库、不复用、无身份**；唯一残渣是"漏了哪些"被冻成 `evidence` 文本 |
| **起步话题 starter topic** | 一次性 | Plan 的引子；崩了才生复习项，过了不留痕 |
| **题型 / 具体题目** | 一次性 | 题型是固定闭集（解释 / 追问 / 迁移 / 复述）；题目用完即弃 |
| **覆盖度清单** | 一次性（per-Goal） | 一场战役内"碰过没"的清单，随战役生灭，不进账本（§4） |
| **掌握度 / 能力 map / 就绪度** | 派生视图 | 复习项状态的聚合，**不是每概念存一个分** |
| **Today Queue / 提醒** | 派生视图 | 账本按 `next_due` 的渲染 ＝ 首页"今日复习"块。**首页本身是 act-led**，另含"继续上次 / 学点新的"两个非账本入口（§3④） |
| **source_ref** | 指针 | 指回原文，不是实体 |
| ~~概念 / 能力维度 / 能力点~~ | **禁用词** | 会勾回本体。"概念"只许出现在否定句（"不建概念图"）；单元一律说"复习项"或"起步话题" |

---

## 2. 数据原语：Goal

一个对象统一两层，避免两套代码：

| | 日常底座 | 面试战役 |
|---|---|---|
| Goal 类型 | 无 deadline、持续 | 有 JD 范围 + deadline + 加密节奏 |
| 来源 | 你导入的任意资料 | JD + 简历 + 面经 + 资料 |
| 节奏 | 每天到期复习 | 倒计时驱动、强度更高 |
| 引擎 | **同一个** | **同一个** |
| 状态库 | **同一个** | **同一个** |

战役结束后，掌握度已沉进同一状态库 → 自动流回日常底座。**面试努力不蒸发。**

> **复习项是全局、持久的，不归属任何 Goal**；Goal 只提供一个 scope（这场战役关心哪些话题）。这正是战役结束后复习项能留下、流回日常的原因，也让"同时两场面试"天然共享同一批薄弱点。**覆盖度清单则相反——per-Goal、随战役生灭**（见 §4 与「面试战役」节）。

---

## 3. 主流程：4 个页面，1 条主线（不做多模式并行）

> "不做多模式并行" 指**不长出第二个引擎 / 第二条平行产品线**，不是禁止视图。面试战役有**自己的仪表盘页**（campaign 型 Goal 的详情视图），但它不是第 5 种模式——drill 仍走下面同一条主线（§1.3 安全测试）。

### ① Setup — 选目标 + 导入材料
- 用户只做两件事：选目标岗位 / 学习目标；导入材料。
- 材料优先级：`JD → 简历 → 高频面经 / starter pack → 补充资料`。
- **不要**让用户先"建知识库""整理文件夹""配语音"。

### ② Plan — 近乎隐形的跳板（不是高潮）
- 只产出**一份"今天就能开始的练习队列"**（几个起步话题即可）。**不产出全量概念地图**——模型主要从 Practice 自下而上长出来（§1.6 / §4）。
- Plan 同时依赖 LLM 又依赖内容，是最薄弱的一步，极易产出"正确的废话"。**把它压到几乎看不见，一个可跳过的轻量跳板**，省下的注意力全给 Practice。

### ③ Practice — 产品本体（60% 的力气都在这）
- **两个入口、同一个引擎**：① **主动学习** —— 你带 / 挑一份原文，就着它读 + drill（吸收现 `learn-workspace`）；② **复习 / 面试** —— 账本到期项或战役话题的对抗式追问（吸收现 `loopassist`）。**日常主动学习是留存引擎，面试只是它的参数化**（守 §1.3；与 §0/§1.1 的"身份决定留存"一致）。
- **现 `/learn` 的"弱阅读 helper + 强单页练习流程"结构保留，不改 UI**：阅读 helper（你问它、带出处）= §1.4 **贬值区**，**够用就行、不投入**；drill（它问你）= 脊柱、价值与账本所在。两者权重差很大，但现状已是如此，不需要动结构。
- **真正改的是底层，不是布局**：① 判分输出从"掌握度 %"→ 锚点命中/漏，漏的写**全局失败账本**（§0.1 / §4）；② 记忆形状从 `capability-memory` 的 concept-keyed 打分图 → 失败为单元的扁平账本（§6）；③ 输入按 §0.2。
- 每个 session 默认 **10–15 分钟**，只围绕 **1 个目标**（一个到期复习项，或 Plan 给的一个起步话题）做 **3–5 轮 drill**。
- 题型固定为少数几类：
  - `解释题`：你能不能讲清。
  - `追问题`：你是不是只会背定义 —— 讲完像面试官一样追问，挖到你讲不下去。（LoopAssist 的灵魂溶解在这里）
  - `迁移题`：换个场景还会不会。
  - `复述题`：隔几分钟还能不能自己说回来。
- **面试模式 = 这里的一套题型参数**，不是独立产品：语气更强、追问更密、更看口头表达、更少教学提示。
- 输入遵循 §0.2（一次性提交；倒计时仅面试模式）。每轮产出：`命中/漏掉哪几个一次性判分锚点 + 暴露的薄弱点`（非裸分数，§0.1）。

### ④ Today Queue — 首页 / 唯一的家
- **首页以"下一个动作"领衔，不是复习作业本**。三件事并列：
  - **继续上次**：没练完的会话，源层续播（"读到 40%，接着读"）。
  - **学点新的**：带一篇新原文 / 从已导入素材池挑一篇，就着它开练 —— **这是主动学习入口，日常留存的主力，给它和复习同等地位**（§1.1 身份层）。
  - **今日复习**：账本里 `next_due` 到期的复习项，**编织进来、不当唯一头条，但必须露脸** —— 它是护城河（§4），埋了就退化成"又一个会聊天的 LLM 壳"。
- （若有进行中的面试战役）追加倒计时 + 覆盖率 + 今日薄弱点。
- 练完不做大而全报告，只给下一步：哪几个点过线 / 哪几个必须复习 / 明天该练什么 / 回看哪段原文。
- **不是文档库、不是稍后读列表、不是图谱、不是 dashboard**。已导入文档是**素材池**（藏在"学点新的"后面），不是可浏览的首页 backlog —— 那只会变成"稍后读坟场"，制造囤积负罪感、不产生留存。

---

## 4. 复习项账本（确定性层 · 护城河资产）

> **核心决策：自下而上建模"失败"，而非自上而下建模"资料"。** 不预拆文档、不建概念图。
> 这不是在"概念建模 vs 维护 agent 上下文"里二选一——纯靠 agent 上下文并没逃开建模：一旦要做跨时间复习调度 + 控制上下文长度，上下文会被迫压缩成一个**隐式、有损、会漂移、不可审计**的概念模型，属性更差，而且把状态重新塞回 LLM 的怀里（违反 §1.2 与 §1.4 的护城河）。所以选**显式但极小**的账本。

### 分工：Agent 管对话，账本管日历
| 职责 | 谁来做 |
|---|---|
| 一轮 drill 内的追问、顺线索挖、按一次性锚点判分 | **Agent（LLM）+ 近期上下文** |
| 跨会话"什么到期该复习"、掌握度真相 | **确定性的复习项账本** |

近期 transcript 只用于连续性 / 适应性，**不是 system of record**，可随意开窗 / 总结 / 丢弃——重要残渣（你答崩了什么）已抽进账本。上下文无限增长的问题随之消失。

### 复习项 = 模型的全部（扁平列表，无图、无边、无本体）
每项只需：
- `handle`：一句人话（"讲清 ReentrantLock 与 synchronized 的区别与取舍"）
- `source_ref`：指回原文那一段（供回看）
- `evidence`：你答崩的那道题 + 没命中的一次性判分锚点（来自 §0.1）
- `state`：`shaky | solid`（两三档即可）
- `last_seen_at` / `next_due_at`

### 每个 session 的回路
1. **调度器（确定性）** 把到期复习项拉成今日队列。
2. **Agent** 拿 `某项 + 原文 + 近期上下文` 跑 drill：判定还 `shaky` / 转 `solid` / 冒出新薄弱点就**新建一项**。
3. **确定性逻辑** 写回 `state` 与 `next_due`。

### 纯失败驱动；"练过"不留痕
- **只有答崩才生复习项。** 一次过的话题**不写任何持久状态**——否则"每碰一个就建一个"会滑回"给遇到的一切建模"。
- 掌握度不是每概念存分，而是**复习项状态的派生视图**。
- **文档级状态分两类、命运相反**：**阅读进度 /「在读」续播保留**（源层书签，"读到哪接着读"，便宜且有用）；**per-doc「已掌握」删除**——你从不掌握一篇文档，只掌握 / 答崩里面的点。顶多留「**已读完**」作为覆盖事实（碰过），但**不准它冒充"会了"**（否则就是 §0.2"会背不会讲"被 UI 伪装掉，也是本节"覆盖度≠掌握度"的犯规）。
- 但面试战役要回答"这份 JD 覆盖完了吗" → 用一个**挂在 Goal 上、随战役生灭的覆盖度清单**（JD 起步话题逐个标 未碰 / 已碰 / shaky / solid）；它只在一场战役内有效、用完即弃、彼此不去重、**不进知识账本** → 是 Goal 的属性，不是第三个知识实体。

### 已知软肋（低风险，可接受）
- "这次薄弱点和上次是不是同一个"靠 agent 判断，有模糊性 → 偶尔重复建项。代价是队列里多一行**冗余，不是错误状态**；新建项时让 agent 扫一眼现有项做匹配即可兜住。对比纯 agent 上下文那种"整个进度都漂移"的高风险模糊，量级完全可接受。
- **复习调度仍是真设计题**：state 虽简化为 shaky/solid，但"按掌握度 + 遗忘排 `next_due`"不能直接套 SM-2/FSRS（它们假设原子卡片 + 二元回忆）。当成专门的设计任务对待。

### 提醒
- **提醒 = 账本的一个视图**（Today Queue），不是独立产品线。外部渠道分发（飞书 / 微信）默认不做。

---

## 面试战役 · 全路径与系统承接

> 面试战役 = 一个带 deadline 的 Goal，复用同一引擎（守 §1.3 护栏），但**有自己的仪表盘页**（纯视图、零自有引擎/状态，见 §1.3 安全测试）。下面是一场面试从立项到收尾的全路径：左边用户在干嘛，右边系统必须承接什么。

### 路径总览（一场面试的生命周期）

| 阶段 | 用户动作 | 系统承接 |
|---|---|---|
| **① 立项** | "我 X 公司 Y 岗、D 日面试" | 建一个 `type=campaign` 的 Goal：`deadline=D`、`scope=JD`。复用已存简历，没有就传 |
| **② 定范围**（Plan，近乎隐形） | 贴 JD，确认 / 补简历 | JD 拆成**扁平起步话题清单**（不是概念图）；与简历交叉、缺口优先 → 产出 (a) per-Goal 覆盖度清单 (b) 起步练习队列 |
| **③ 冲刺**（每天） | 每天 10–15 min drill | **deadline 感知调度**：今日队列 = 未覆盖话题 + 到期复习项，按"风险 × 剩余天数"排序；崩了生复习项，过了标覆盖 |
| **④ 模拟**（可选 · 连播） | 来一场完整模拟面 | 选代表集（未覆盖 + shaky + 几个 solid 壮胆）连播、压力参数、无教学提示 → 出**就绪度读数**（覆盖率 / shaky 清单 / 最可能被问崩的点） |
| **⑤ 面试前夜** | 最后过一遍 | 只推**还 shaky 的 + 最高风险的**，快速过一轮、给信心 |
| **⑥ 面试当天** | 进场前扫一眼 | 给**你自己的 shaky 点清单**（不是背诵稿、不是实时提示）——这是合法准备，不是作弊 |
| **⑦ 面试后**（最易被漏的承接） | 回报：被问了啥、哪崩了 | 把"真被问到却答不上的"**转成新复习项**（真实失败最值钱）→ 写进账本 |
| **⑧ 收尾** | 拿到 offer / 凉了 | 归档 Goal、丢弃覆盖度清单；**复习项留下**（本就全局）→ 流回日常底座 = 留存桥 |

### 几个关键承接，单独点名

1. **deadline 感知调度（③）**：与日常模式**同一批复习项、同一引擎**，调度函数只多吃一个 deadline——
   - `next_due` 被 deadline **封顶**（面试在 3 天后，就不能把复习排到 7 天后）；
   - 临近 deadline 或覆盖落后 → **压缩间隔、加大每日量**。
   - 这就是"参数化"的全部，**不是新引擎**（守 §1.3）。
2. **覆盖度 ≠ 掌握度**：覆盖度清单（per-Goal、临时）答"碰过没"；账本（全局、持久）答"会不会"。**碰过 ≠ 会**——一次过的只标覆盖、不写持久掌握。
3. **面试后回灌（⑦）**：一个专门的"回报"入口，把现实失败转成复习项。多数面试产品漏掉、但价值最高的一步，也是"两场面试共享薄弱点 / 战役沉淀进日常"的来源。
4. **绝不发"背诵答案"**：当天清单是**你自己的薄弱点**，不是标准答案稿。产品训练你**当场生成**而非背脚本——既守住"会讲不会背"的哲学，也守住不踩作弊线。
5. **留存桥不是 nice-to-have，是结构必需**：面试的 deadline 是**终结性**的——面完动机即蒸发，**只做面试准备的产品每场面试后近乎 100% 流失**（漏水的桶）。唯一的堵漏方式就是 ⑦⑧：真实失败转复习项、战役结束**无缝落回日常 Today Queue 且账本已装着这场战役的残渣**。**面试与日常练习机制几乎相同，不是要消解的混乱，恰是钩子能零成本转留存的机制**——拔掉 deadline 与 JD 范围，同一回路继续跑。

### 就绪度读数:达标度怎么算（派生视图,不是新实体）

回答用户最想知道的一句:**"我离这份 JD 还差多少?"** 它是覆盖度清单 + 失败账本的聚合,不新建任何持久实体。

- **输入**:覆盖度清单(per-Goal,JD 拆扁平话题,与简历交叉——简历声称会的优先验、缺口优先补);每个话题的状态取自失败账本 + 覆盖(`未覆盖 / 生疏 / 扎实`);话题重要度(JD core vs secondary)。
- **输出**(一个读数,不是一堆分):
  - **覆盖率** = 碰过的话题 / JD 要求的话题(答"碰没碰")
  - **达标度** = 扎实话题(按重要度加权)/ 要求话题(答"会不会")
  - **还差哪些** = 未覆盖 + 生疏,按 `重要度 × 风险 × (1/剩余天数)` 排序
  - **最可能被问崩** = 上面排序的头几个
- **诚实边界(必须写,否则误导)**:
  - 达标度只和两件事一样准——① 锚点判分质量(已测,见 ARCHITECTURE)② JD→话题的拆解质量(**这是全链最弱一环**,就是 §1.6 警惕的"拆解";但它 per-Goal、随战役生灭、用完即弃,所以可接受)。
  - **覆盖率 ≠ 达标度 ≠ 真能过面**。它是"你自己的薄弱点地图",不是录用预测。**别把它做成一个唬人的百分比当卖点。**

### 面到死模式（relentless mock,失败收割机）

- **本质**:模拟连播(④)关掉早停 + 对抗参数拉满。**不是新模式,是"模拟面"的一个强度档**(守 §1.3:同一出题+判分引擎,只改参数——无早停、追问密度 max、去教学提示)。
- **干嘛用**:面试前一次性把脆弱面全照出来。顺着 JD 高风险话题一路追问到你讲不下去,把所有撑不住的点 → 全部转复习项灌进账本。
- **为什么有价值**:真面试官就是追问到你崩(§3③ 追问题);面到死把这个跨整个 JD 范围铺开,是最快的失败收割。
- **收尾必须给就绪度读数**:不能让你一路失败然后走人。结束给"你扛到哪 / 哪些崩了 / 明天先补哪几个"——否则就是纯打击,反留存。

### 边界（这一段明确不做）
- 不做面试当天的**实时识别 / 实时喂答案**（已砍，§7）。
- 公司情报 / 面经抓取**最多是可选输入**，不是核心。
- 不自动生成"标准答案"给你背。

---

## 5. 资源配比（极端聚焦）

| 比重 | 模块 | 内容 |
|---|---|---|
| **60%** | 练习引擎 | 题型、追问策略、判分质量、救援材料 |
| **25%** | 记忆与复习层 | 锚点判定、掌握状态、复习队列、复练策略 |
| **10%** | 导入与资料处理 | 够用就行（这是贬值区，别投入） |
| **5%** | 语音 / 实时辅助 / 渠道提醒 | 默认非核心，Labs / 可选插件 |

---

## 6. 现有代码 → 目标映射

| 现状 | 去向 |
|---|---|
| `ai-service/app/engine`（session_engine / tutor_intelligence / mastery_scoring） | **保留并强化** → Practice + 状态层的内核 |
| `learn-workspace`（阅读 + drill） | **吸收为 Practice 的主动学习入口**；阅读 + 追问壳子大量保留，去掉 per-doc 阅读状态与"整篇拆训练点当真实状态"（拆解降为一次性展示，不入库不流转） |
| `home-page`（文档库） | 重构为 **act-led 首页**（继续 / 学点新的 / 今日复习）；文档库降为**素材池**，不再是首页 |
| `loopassist`（corpus / service） | 灵魂（对抗式追问）**溶解进 Practice 题型**；corpus 降为 starter 内容 |
| `loopassist` TTS / Qwen3 / 阿里云 | **降为可选插件，默认关** |
| `interview_assist` + `livekit-agent` + `aliyun_realtime_asr` | **移出主仓库** → Labs 或独立 repo |
| `src/superapp` + `reminder-candidate` | 改造为 Today Queue 视图；**砍掉外部渠道分发** |
| `src/tutor/capability-memory` / `src/mastery` / `src/user` | 确定性状态层 → **复习项账本**（§4，失败驱动）；重点补强（你说"做得不够"的地方） |
| `resume-version-store` + `/api/interview/start-target` | Setup（JD+简历导入）+ Goal 原语 |
| `baseline-packs` / JavaGuide | 降为 **starter pack**；命名去 Java 化 |

---

## 7. 砍 / 降 / 缓

- **砍出主仓库**：实时面试辅助（interview_assist（已迁移到 ../InteviewAssistAI））。技术展示感强，但稀释价值 + 带作弊联想 + 延迟难做。
- **降为可选插件（默认关）**：语音 / TTS。灵魂已进 Practice，躯壳可拔。
- **降为 starter pack**：JavaGuide。别再让命名和代码被 Java 面试绑死。
- **缓**：飞书 / 微信渠道提醒。先做成 §4 的站内队列。

---

## 8. MVP：一条零重依赖的主链路

**先把这条做出来、且只用一个 LLM API key 就能跑（不装 ML / TTS / LiveKit）：**

```
导入材料 → 生成今日队列 → 开始一轮 drill → 答崩写进账本 → 首页出现明日队列
```

- Hero 场景：技术面试准备。
- 输入：JD + 简历 + 若干资料。
- 核心动作：每天 10 分钟 drill。
- 核心产出：今日队列 + 薄弱点 + 下次复习。
- 可选模式：`Interview pressure mode`（题型参数化）。
- 一切重依赖（语音 / 实时 / 渠道）进 Labs。

> 对开源个人项目，"陌生人 clone 后 5 分钟内能跑通一次 loop"比任何功能取舍都更决定生死。

---

## 9. 开源可跑版 vs 自用增强版

| | 开源可跑版（默认） | 自用增强版（flags / Labs） |
|---|---|---|
| 主链路 4 页 | ✅ 纯文本 | ✅ |
| 依赖 | 一个 LLM key | + ML / TTS / LiveKit |
| 语音面试官 | ❌ | ✅ 可选插件 |
| 实时面试辅助 | ❌ | ✅ Labs（或独立 repo） |
| 渠道提醒 | ❌（站内队列） | ✅ 飞书 / 微信 |
| starter 内容 | 内置 JavaGuide pack | + 自有面经语料 |

---

## 北极星 & 反指标

- **北极星**：用户每天回到 Today Queue 并完成至少一轮 drill 的留存（而非注册数 / 资料导入数）。
- **反指标**：
  - 任何把首页变成"知识库 / 图谱 / dashboard"的冲动。
  - 任何让面试模式长出独立引擎的改动（违反 §1.3 护栏）。
  - 任何为"内容生成/问答"加重投入的计划（§1.4 贬值区）。
  - 任何"先把资料拆成完整概念图"的计划（违反 §1.6 / §4：失败驱动、不建模资料）。
  - 任何把"覆盖度清单 / 练过 / 锚点"做成持久实体的改动（违反术语表：只有 Goal 与复习项持久）。
  - 任何把首页变回"文档库 / 稍后读列表 / 可浏览 backlog"的冲动（已导入文档只是素材池，§3④）。
  - 任何给文档挂 per-doc"已掌握"标记的改动——掌握只能是账本的派生视图（§1.5 / §4）。
