# 对话引擎上下文管理、提示词与工程编排总览

本文档面向当前仓库的**现行 split-service 对话引擎**做整体梳理，重点覆盖：

- 上下文管理
- 系统提示词原文与参数来源
- 工程编排流程
- 工程侧规范化、校验、投影处理

补充说明：

- 本文以 `ai-service + bff + frontend` 为主线。
- `src/tutor/*` 与 `src/server.js` 中的 legacy Node 链路仍然存在，本文在末尾给出对照说明。
- 按本轮 review 约定，**模型能力限制本身不纳入评估**；这里只梳理“当前工程如何向模型提供上下文、如何接收并处理模型输出”。

---

## 1. 目录与主链路

### 1.1 当前生效的 split-service 主链

- `frontend/components/app-shell.js`
  - 前端会话页、提交回答、按钮意图
- `bff/src/server.js`
  - 账号、baseline、会话请求代理
- `ai-service/app/main.py`
  - `/api/interview/*` HTTP 入口
- `ai-service/app/engine/session_engine.py`
  - 会话编排主逻辑
- `ai-service/app/engine/context_packet.py`
  - 构建 LLM 上下文包
- `ai-service/app/engine/tutor_intelligence.py`
  - Provider 适配、提示词、schema、normalize
- `ai-service/app/engine/turn_envelope.py`
  - turn envelope 校验、控制裁决、转 tutor move
- `src/view/chat-transcript.js`
  - 工程 turn -> 可见 transcript 投影
- `src/view/visible-session-view.js`
  - session -> visible view 聚合

### 1.2 Legacy Node 对照链

- `src/server.js`
- `src/tutor/session-orchestrator.js`
- `src/tutor/context-packet.js`
- `src/tutor/tutor-intelligence.js`
- `src/tutor/turn-envelope.js`

当前 split-service 基本上是把上述 Node 逻辑拆到 Python service + BFF + Next 前端里。

---

## 2. 请求入口与外部契约

### 2.1 AI Service HTTP 入口

文件：`ai-service/app/main.py`

#### `POST /api/interview/start-target`

请求模型：

```python
class StartTargetRequest(BaseModel):
    userId: str = ""
    source: Dict[str, Any]
    decomposition: Dict[str, Any]
    targetBaseline: Dict[str, Any]
    memoryProfile: Dict[str, Any]
    interactionPreference: str = "balanced"
```

说明：

- `source`: 当前学习源/面试包文本来源
- `decomposition`: 预先拆好的知识点集合
- `targetBaseline`: 目标岗位/题包元信息
- `memoryProfile`: 长期记忆快照
- `interactionPreference`: 当前交互风格，当前枚举为：
  - `probe-heavy`
  - `balanced`
  - `explain-first`

#### `POST /api/interview/answer`

请求模型：

```python
class AnswerRequest(BaseModel):
    sessionId: str
    answer: str
    intent: Optional[str] = None
    burdenSignal: str = "normal"
    interactionPreference: Optional[str] = None
```

说明：

- `answer`: 自由文本回答
- `intent`: 显式结构化控制意图，当前支持：
  - `teach`
  - `advance`
- `burdenSignal`:
  - `normal`
  - `high`

#### `POST /api/interview/focus-domain`

```python
class FocusDomainRequest(BaseModel):
    sessionId: str
    domainId: str
```

#### `POST /api/interview/focus-concept`

```python
class FocusConceptRequest(BaseModel):
    sessionId: str
    conceptId: str
```

### 2.2 BFF 作用

文件：`bff/src/server.js`

职责：

- 用户登录/建号
- 读取/保存 `memoryProfile`
- 根据 `targetBaselineId` 组装：
  - `source`
  - `decomposition`
  - `targetBaseline`
- 把前端请求代理到 AI Service
- 在返回体中附带 `traceId`

### 2.3 前端输入行为

文件：`frontend/components/app-shell.js`

当前输入分为两类：

- 自由文本回答
  - `answer = textarea.value`
  - `intent = ""`
- 显式按钮动作
  - `讲一下` -> `intent = "teach"`
  - `下一题` -> `intent = "advance"`

也就是说：

- **按钮**走结构化意图
- **自然语言**走模型理解

---

## 3. 会话对象与上下文管理

### 3.1 Session 的核心字段

文件：`ai-service/app/engine/session_engine.py`

编排中的核心会话字段包括：

- `currentConceptId`
- `currentProbe`
- `currentQuestionMeta`
- `turns`
- `engagement`
- `workspaceScope`
- `runtimeMaps`
- `memoryProfile`
- `revisitQueue`
- `latestControlVerdict`
- `interactionLog`

### 3.2 `project_session()` 对外投影

文件：`ai-service/app/engine/session_engine.py`

AI Service 返回给上游的主要会话字段：

- `sessionId`
- `concepts`
- `currentConceptId`
- `currentProbe`
- `currentQuestionMeta`
- `masteryMap`
- `nextSteps`
- `turns`
- `engagement`
- `workspaceScope`
- `currentRuntimeMap`
- `currentMemoryAnchor`
- `targetMatch`
- `abilityDomains`
- `memoryEvents`
- `latestMemoryEvents`
- `interactionLog`
- `latestFeedback`
- `memoryProfileSnapshot`

### 3.3 上下文管理的三层结构

当前上下文包由 `build_context_packet()` 构建，分为三层：

#### A. Stable（稳定层）

描述不会在一个小回合里剧烈变化的信息：

- target
- scope
- anchor identity
- memory anchor

#### B. Dynamic（动态层）

描述当前这一轮的回合状态：

- 当前题目
- learner 当前输入
- 当前负荷
- 最近通用 turns
- 当前锚点的教学历史 `anchorHistory`
- 最近 evidence
- 当前轮 draft evidence

#### C. Reference（参考层）

描述外部知识与材料引用：

- provenance
- JavaGuide 知识源
- remediation materials

### 3.4 当前上下文包结构

文件：

- Python: `ai-service/app/engine/context_packet.py`
- Legacy JS: `src/tutor/context-packet.js`

当前会同时提供：

- 分层结构：
  - `stable`
  - `dynamic`
  - `reference`
- 以及 prompt 友好的平铺 alias：
  - `target`
  - `scope`
  - `anchor`
  - `memory_anchor_summary`
  - `recent_evidence`
  - `recent_turns`
  - `anchor_history`
  - `source_refs`
  - `runtime_understanding_map`
  - `budget`
  - `friction_signals`
  - `stop_conditions`
  - `draft_evidence`

### 3.5 `anchor_history` 设计

这是当前为“同题补缺口而不是重讲全文”新增的核心上下文字段。

#### Python 形态

```json
{
  "recent_turns": [
    {
      "role": "tutor|learner",
      "kind": "question|feedback|answer|control",
      "action": "teach|check|advance|...",
      "content": "...",
      "takeaway": "..."
    }
  ],
  "teach_count": 1,
  "has_recent_teaching": true,
  "recent_takeaways": ["...", "..."]
}
```

#### 作用

- 告诉模型“这题最近是否已经完整讲过”
- 告诉模型“上一轮收口结论是什么”
- 给模型做“增量补位”提供显式材料

### 3.6 `interaction_context` 的现状

当前没有单独命名成 `interaction_context`，但实际信息分散在：

- `dynamic.engagement`
- `friction_signals`
- `stop_conditions`
- `intent`
- `workspaceScope`

也就是说，当前已经有“交互上下文”，只是还没被抽成一个独立 contract 名字。

---

## 4. 系统提示词原文与参数来源

这里把**当前 split-service 生效的提示词**按调用类型列出来。

### 4.1 公共 system prompt

来源：

- 文件：`ai-service/app/engine/tutor_intelligence.py`
- 方法：`ProviderTutorIntelligence._call_json_traced()`

原文：

```text
You are the cognition layer for an AI tutor. Return valid json only. Use the submitted material as the primary anchor, but you may use necessary background knowledge to teach clearly. Never drift into generic motivational talk.
```

适用范围：

- `decompose`
- `answer_turn`
- `explain_concept`

公共参数：

- `call_type`
- `model`
- `provider`
- `parser_version`
- `schema`
- `validator`

---

### 4.2 Prompt 1: `decompose_source`

来源：

- 文件：`ai-service/app/engine/tutor_intelligence.py`
- 方法：`ProviderTutorIntelligence.decompose_source()`

原文模板：

```text
Read the submitted learning material and produce 3-7 document-local teachable units.
Requirements:
- Stay anchored to the submitted source, but use minimal background knowledge when needed for clearer teaching.
- Do not leak frontmatter, tags, SEO metadata, or boilerplate into the learner-facing summary.
- Each unit must support a concrete first diagnostic question.
- Each unit should include a check question for teach-back after explanation.
- Prefer mechanisms, distinctions, failure modes, and misconceptions over broad topic labels.
- Assign importance as core/secondary/optional and coverage as high/medium/low.

TITLE: {SOURCE_TITLE}
URL: {SOURCE_URL?}
CONTENT:
{SOURCE_CONTENT}
```

参数来源：

- `{SOURCE_TITLE}`: `source.title`
- `{SOURCE_URL}`: `source.url`
- `{SOURCE_CONTENT}`: `source.content`

输出 schema：

- `DECOMPOSITION_SCHEMA`

用途：

- 把 source 拆成 `concepts[]`
- 为后续对话提供每题的：
  - `summary`
  - `excerpt`
  - `keywords`
  - `diagnosticQuestion`
  - `retryQuestion`
  - `checkQuestion`

---

### 4.3 Prompt 2: `answer_turn` / turn envelope 主推理 prompt

来源：

- 文件：`ai-service/app/engine/tutor_intelligence.py`
- 方法：`build_turn_envelope_prompt()`

原文模板：

```text
You are the main reasoning engine for one AI tutor turn. Return json only.
Use Chinese in all visible learner-facing text.
Follow this internal order: first update runtime_map, then decide next_move, then write the reply, then propose writeback_suggestion.
Preserve prior hypotheses unless new evidence explicitly refutes them.
The runtime_map must stay anchored to the current anchor_id and cite evidence ids where possible.
Do not ask repetitive probes when info_gain_level is negligible or stop_conditions discourage more probing.
Budget, friction_signals, and stop_conditions are orchestration factors. Consider them before proposing continued probing or verification.
The reply must sound like a strong human tutor, not like a template or checklist.
Interpret the learner utterance pragmatically: it may contain an answer, a request for explanation, a request to move on, or a mix of these.
When the learner utterance is mainly asking for explanation or summary rather than offering substantive evidence, prefer a closure-oriented helpful reply instead of a bare acknowledgement plus another question.
Prefer incremental tutoring over repetition. If recent turns on the same anchor already explained the core mechanism, do not restate the whole explanation unless the learner is still clearly lost; instead name the one missing link, add at most one new example, and move to a narrower follow-up.
Avoid repeatedly opening with stock transitions such as '进入学习模式' or similar phrases if recent turns already used them.
Use anchor_history to understand what has already been taught on the current anchor; if it contains recent tutor explanations, treat them as prior teaching context rather than repeating them verbatim.
When teach is the right move, teaching_paragraphs must contain a complete explanation; do not use rigid headings such as 核心结论 or 理解抓手.
When you choose verify, the visible reply should still add concrete value before any follow-up question.
When a response is still needed on the current anchor, next_prompt must be a concrete question the learner can answer immediately.
Treat next_prompt as a candidate follow-up only for staying on the current anchor. If the turn should hand off to a different anchor or stop, leave next_prompt empty.
When long-term memory should not be updated, set writeback_suggestion.should_write to false and mode to noop.

CONTEXT_PACKET_JSON:
{CONTEXT_PACKET_JSON}

CURRENT_LEARNER_INPUT: {ANSWER}
```

参数来源：

- `{CONTEXT_PACKET_JSON}`: `build_context_packet(session, concept, answer, ...)`
- `{ANSWER}`: 当前 `payload.answer`

输出 schema：

- `TURN_ENVELOPE_SCHEMA`

输出四段：

- `runtime_map`
- `next_move`
- `reply`
- `writeback_suggestion`

---

### 4.4 Prompt 3: `explain_concept`

来源：

- 文件：`ai-service/app/engine/tutor_intelligence.py`
- 方法：`ProviderTutorIntelligence.explain_concept()`

原文模板：

```text
You are generating a compact study card for a tutoring product. Return json only.
The learner explicitly clicked a control meaning 'teach me this point now'.
Use Chinese in all visible text.
Ground the explanation in the concept and the provided JavaGuide snippets.
Do not produce generic motivation. Produce a concise but genuinely useful learning card that can stand on its own even if the learner never opens the source articles.

TARGET: {TARGET_TITLE}
CURRENT CONCEPT: {CONCEPT_TITLE}
CONCEPT SUMMARY: {CONCEPT_SUMMARY}
CONCEPT EXCERPT: {CONCEPT_EXCERPT}
MISCONCEPTION: {MISCONCEPTION}
REMEDIATION HINT: {REMEDIATION_HINT}
CHECK QUESTION: {CHECK_QUESTION}
CURRENT QUESTION: {CURRENT_QUESTION}
CONTEXT_PACKET: {CONTEXT_PACKET_JSON}
GUIDE_SNIPPETS_JSON:
{GUIDE_SNIPPETS_JSON}

Requirements:
- visibleReply should sound like a tutor switching into study mode
- teachingParagraphs must be a complete teaching explanation, not a note stub
- teachingParagraphs should feel like a short live explanation from a strong tutor, not like a checklist
- if the learner has already seen a full explanation for this anchor in recent turns, compress to the missing link instead of repeating the whole lecture
- cover the concept definition, mechanism, boundary/contrast, and the most common misunderstanding, but do it naturally rather than through forced section headers
- use the JavaGuide snippets as supporting references only; do not let the output degrade into a list of article titles
- never use rigid labels such as '核心结论' or '理解抓手' or '建议阅读' as section headers
- return 2-4 short natural paragraphs in teachingParagraphs
- checkQuestion should force a teach-back in the learner's own words
- takeaway should be a single stable sentence
```

参数来源：

- `{TARGET_TITLE}`: `session.targetBaseline.title` 或 `session.source.title`
- `{CONCEPT_TITLE}`: `concept.title`
- `{CONCEPT_SUMMARY}`: `concept.summary`
- `{CONCEPT_EXCERPT}`: `concept.excerpt`
- `{MISCONCEPTION}`: `concept.misconception`
- `{REMEDIATION_HINT}`: `concept.remediationHint`
- `{CHECK_QUESTION}`: `concept.checkQuestion || concept.retryQuestion`
- `{CURRENT_QUESTION}`: `session.currentProbe`
- `{CONTEXT_PACKET_JSON}`: 当前锚点上下文包
- `{GUIDE_SNIPPETS_JSON}`: `load_java_guide_source_snippets(...)`

输出 schema：

- `EXPLAIN_CONCEPT_SCHEMA`

---

### 4.5 Legacy Node 对照提示词

文件：`src/tutor/tutor-intelligence.js`

现状：

- OpenAI 与 DeepSeek legacy prompt 基本保留
- 文本与 Python 主链大体同构
- 但 legacy 里还保留：
  - `reviewTurn`
  - `generateTutorTurn`
  - heuristic intelligence

这意味着 legacy 链路的 prompt surface 更宽，split-service 当前主用的是：

- `decompose_source`
- `generate_turn_envelope`
- `explain_concept`

---

## 5. 工程编排流程

### 5.1 `start-target` 编排

入口：

- BFF: `bff/src/server.js#handleStartTarget`
- AI Service: `ai-service/app/main.py#start_target`
- 编排：`ai-service/app/engine/session_engine.py#create_session`

流程：

1. BFF 读取用户与 `memoryProfile`
2. BFF 用 baseline pack 组装：
   - `source`
   - `decomposition`
   - `targetBaseline`
3. AI Service `create_session()`
4. 初始化：
   - `concepts`
   - `conceptStates`
   - `ledger`
   - `runtimeMaps`
   - `workspaceScope`
   - `turns`
5. 生成第一题 `currentProbe`
6. 返回 `project_session()`

### 5.2 `answer` 编排

入口：

- `ai-service/app/main.py#answer`
- `ai-service/app/engine/session_engine.py#answer_session`

主流程：

1. 读取当前 `concept`
2. 解析显式/隐式控制意图
   - `detect_control_intent(answer, intent)`
3. 写入 learner turn
4. 如果是控制意图：
   - `teach` -> `handle_teach_control()`
   - `advance` -> `handle_advance_control()`
5. 否则进入正常回答链：
   - 构建 `context_packet`
   - 调用 `generate_turn_envelope`
   - `normalize_turn_envelope_payload`
   - `assert_valid_turn_envelope`
   - `assert_consistent_turn_envelope`
   - `turn_envelope_to_tutor_move`
6. 将结果写回：
   - `ledger`
   - `runtimeMaps`
   - `conceptStates`
   - `memoryProfile`
7. 计算：
   - `controlVerdict`
   - `turnResolution`
   - `latestFeedback`
8. 选择下一题：
   - `choose_next_unit()`
9. 追加 tutor feedback turn
10. 如有 `currentProbe`，再追加 tutor question turn
11. `project_session()`

### 5.3 `focus-domain` / `focus-concept`

入口：

- `ai-service/app/main.py`
- `ai-service/app/engine/session_engine.py#apply_focus_domain`
- `ai-service/app/engine/session_engine.py#apply_focus_concept`

流程：

1. 更新 `workspaceScope`
2. 选定焦点 concept
3. 写入 workspace turn
4. 写入新的 tutor question turn
5. 返回新的 `project_session()`

---

## 6. 工程处理层

### 6.1 控制意图处理

文件：`ai-service/app/engine/control_intents.py`

当前逻辑：

- 优先吃结构化 `intent`
- 自然语言仅做兼容：
  - `下一题`
  - `讲一下`

这部分属于**工程入口层**，不是 tutor 内容决策层。

### 6.2 Provider 输出处理

文件：

- `ai-service/app/domain/interview/parsers.py`
- `ai-service/app/engine/tutor_intelligence.py`

处理阶段：

1. `parse_provider_json_text()`
   - 去 code fence
   - 从文本中截取 JSON
2. alias normalize
   - `部分掌握 -> partial`
   - `正向 -> positive`
3. payload normalize
   - `normalize_turn_envelope_payload()`
   - `normalize_explain_concept_payload()`

### 6.3 Envelope 校验

文件：`ai-service/app/engine/turn_envelope.py`

分三步：

1. `assert_valid_turn_envelope()`
   - schema/枚举/必填项校验
2. `assert_consistent_turn_envelope()`
   - 工程一致性校验
   - 例如：
     - `teach` 必须带 `teaching_paragraphs`
     - `advance/stop` 不能还要求回答
3. `turn_envelope_to_tutor_move()`
   - envelope -> 内部 tutor move

### 6.4 `runtime_map` / `next_move` / `writeback_suggestion`

当前语义：

- `runtime_map`
  - 当前锚点的工作记忆 / 诊断态
- `next_move`
  - 模型建议的下一步交互动作
- `writeback_suggestion`
  - 是否把本轮证据写入长期记忆

注意：

- 这些是**模型输出 contract**
- 工程侧会校验，但尽量不替模型做内容决策

### 6.5 Control verdict

文件：`ai-service/app/engine/turn_envelope.py#build_control_verdict`

输入：

- `next_move`
- `reply.requires_response`
- `runtime_map`
- `stop_conditions`
- `budget`

输出：

- `should_stop`
- `reason`
- `scope_type`
- `budget_snapshot`

这是工程层对“是否继续追问”的二次保护。

### 6.6 记忆写回

文件：

- `ai-service/app/engine/session_engine.py#apply_writeback_suggestion`

当前写回对象：

- `memoryProfile.abilityItems[conceptId]`

写回内容包括：

- `state`
- `confidence`
- `reasons`
- `derivedPrinciple`
- `evidence`
- `recentStrongEvidence`
- `recentConflictingEvidence`

---

## 7. 展示层投影

### 7.1 `visible-session-view`

文件：`src/view/visible-session-view.js`

职责：

- 从 session 中抽出：
  - `chatTimeline`
  - `currentProbe`
  - `latestFeedback`
  - `targetMatch`
  - `latestMemoryEvents`
  - `interactionLog`

### 7.2 `chat-transcript`

文件：`src/view/chat-transcript.js`

职责：

- 工程 `turns[]` -> 面向用户的 transcript entries

当前规则：

- workspace turn -> event
- learner turn -> user message
- tutor feedback + same-concept tutor question -> 合并成一个 assistant block
- tutor feedback + cross-concept tutor question -> 不合并
- 条件显示：
  - `takeaway`
  - `followUpQuestion`
  - `candidateFollowUpQuestion`
  - `coachingStep`

### 7.3 `带走一句` 当前策略

当前不是“有 takeaway 就显示”，而是条件展示：

- `teach`
- `advance`
- `abstain`
- 或没有继续追问的 turn

也就是说，它现在更接近“回合收口总结”，不再是每轮必有标签。

---

## 8. 当前对话引擎的整体判断

如果只看工程结构，当前 split-service 对话引擎已经形成了比较明确的层次：

1. HTTP/BFF 入口层
2. Session 编排层
3. Context packet 层
4. Prompt / schema / provider 层
5. Envelope 校验与转换层
6. Memory writeback 层
7. Visible transcript 投影层

但仍有几个值得整体 review 的点：

- `interaction_context` 还没有被正式命名成独立 contract
- `anchor_history` 已经存在，但摘要粒度仍然偏粗
- `turnResolution / nextMove / controlVerdict` 三者的职责边界可以再收敛
- split-service 与 legacy Node 还存在少量提示词和 fallback 行为差异

---

## 9. 建议的 review 重点

这份文档建议重点 review 下面 5 个问题：

1. 当前 `context_packet` 的字段层次是否清晰，是否需要抽出 `interaction_context`
2. `anchor_history` 的摘要是否足够支持“补缺口而不是重讲全文”
3. `answer_turn` prompt 是否已经足够表达通用 tutor policy
4. `explain_concept` 是否应该继续作为独立模式，还是进一步并入统一 turn policy
5. `takeaway / follow-up / candidateFollowUp` 的展示策略是否已经足够轻，不会反过来束缚模型

---

## 10. 附：Legacy Node 说明

Legacy Node 链中仍保留一些额外 surface：

- `reviewTurn`
- `generateTutorTurn`
- heuristic tutor intelligence

对应文件：

- `src/tutor/tutor-intelligence.js`
- `src/tutor/session-orchestrator.js`
- `src/tutor/context-packet.js`

这意味着：

- Python split-service 当前更接近“schema 驱动的主链”
- Node legacy 仍保留更多 fallback/测试双桩能力

如果后续要继续收敛架构，建议把“当前有效策略”继续以 split-service 为准，把 legacy 视为：

- 回归参考
- 行为对照
- 迁移清单来源

